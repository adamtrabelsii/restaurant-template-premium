// Single place to rebrand the template — see BRANDING.md for the full guide.
export const BRAND = {
  // The wordmark. Rendered letter-by-letter in the hero and as <Wordmark />
  // in the navbar, footer and loading screen.
  name: 'ARDOR',

  // Zero-based index of the letter rendered in the accent color.
  // 'ARDOR' with index 3 → the second 'O' is wine-red.
  accentLetter: 3,

  // Namespace for localStorage / sessionStorage keys.
  storagePrefix: 'ardor',
}
